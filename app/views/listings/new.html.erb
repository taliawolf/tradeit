<div>
  <%= simple_form_for [@listing] do |f| %>
    <%= f.input :title %>
    <%= f.input :text %>
    <%= f.input :photos, as: :file, input_html: { multiple: true } %>
    <%= f.button :submit %>
  <% end %>
</div>
